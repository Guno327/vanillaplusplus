# Portable tooling downloaded by scripts, not source
.tools/

# Built pack output (regenerated via scripts/build_mrpack.py /
# scripts/build_server_bundle.py)
*.mrpack
vanilla-plus-plus-server-*.zip

# Python bytecode cache
__pycache__/

# Non-Modrinth mod jars cached/bundled by scripts/build_mrpack.py
# (embedded into the .mrpack's overrides/mods/, not tracked as source)
pack/mods/*.jar

# Server runtime (rebuilt from pack/mods.lock.json via scripts/build_server.py)
server/mods/
server/libraries/
server/versioned_datapacks/
server/world/
server/logs/
server/crash-reports/
server/*.jar
server/*.jar.log
server/eula.txt
server/run.sh
server/run.bat
server/user_jvm_args.txt
server/server.properties
server/banned-ips.json
server/banned-players.json
server/ops.json
server/usercache.json
server/whitelist.json
server/config/
server/kubejs/
server/defaultconfigs/
server/local/
server/cache/
server/patchouli_books/
server/.sable/
server/cmd_fifo

# PM/agent scratch space — idle-watch baselines, feasibility notes, etc.
# Never tracked; ephemeral per-session working files.
.scratch/

# NeoForge/Gradle test runs drop a logs/ dir inside each mod project
# (mods-src/<mod>/logs/) - build artifacts, never committed.
mods-src/*/logs/

# Engineers symlink the gitignored pinned JDK into an isolated worktree as
# `.tools` (a symlink, which the `.tools/` dir pattern above does not match).
.tools
